export interface HSCode {
    id: string;
    code: string;
    name: string;
    description?: string; 
    explanation?: string; 
    dutyPercentage?: number; 
    dutyWtoPercentage?: number;
    permits?: string; 
}
